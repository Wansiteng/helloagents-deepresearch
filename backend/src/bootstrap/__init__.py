"""Process-level bootstrap helpers (proxy reset, monkey-patches).

Import these modules from ``main.py`` *before* any HTTP client is constructed
so the configuration takes effect for the whole process.
"""
